create speaker notes for basic presention on how large language models performing inference
- describe a decode only llm
- one slide notes for each basic phase in inferernce with an llm
- each slide note should have 4-6 bullet points for the speaker
- balance the notes content between intuition vs technical details. lean towards intuitive 
- you can have more than one slide for a given phase if its needed


- the phases of the llm processing:
  - what is a decode only LLM
  - the difference between training and inference
  - tokenization
  - embeddings
  - positional encoding
  - self attention 
  - feed forward layers
  - layer stacking / many transfer blocks in sequence
  - output from vector to words
  - show full workflow of the LLM
  

